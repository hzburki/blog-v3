---
const { tag, count } = Astro.props;
---

<a
  href={`/tags/${tag}`}
  class="rounded-md bg-zinc-100 px-2 py-1 text-xs font-medium text-zinc-700 dark:bg-zinc-800 dark:text-zinc-300"
>
  {tag}
  {count ? `(${count})` : ""}
</a>
